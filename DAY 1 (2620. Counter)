2620. Counter

URL (https://leetcode.com/problems/counter/description/?envType=study-plan-v2&envId=30-days-of-javascript)

solution:
/**
 * @param {number} n
 * @return {Function} counter
 */
var createCounter = function(n) {
    var initalValue = n-1;
    return function() {
        initalValue++;
        return initalValue;
    };
};

/** 
 * const counter = createCounter(10)
 * counter() // 10
 * counter() // 11
 * counter() // 12
 */

Approch:
using clossuer

Notes:

Here I create first varbile to store the initalValue and with the help of clossuer it pass with thier inner function and start incrementing by 1.
